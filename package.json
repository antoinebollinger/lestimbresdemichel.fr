{
  "name": "michel_dumont",
  "version": "1.0.0",
  "description": "Site philatélie Michel",
  "main": "index.php",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch ./sass:./css --style compressed",
    "prefix": "postcss ./css/style.css --use autoprefixer -d ./css/"
  },
  "author": "Antoine Bollinger",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
